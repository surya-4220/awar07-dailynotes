name: Multiple events

on: 
    push: 
        branches: [main, demo]
    pull_request:
        branches: [main]
    workflow_dispatch:

jobs:
  print-msg:
    runs-on: ubuntu-latest
    steps:
      - name: Say my name
        run: echo "My name is GitHub Actions"